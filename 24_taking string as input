// Online C compiler to run C program online
#include <stdio.h>

int main() {
    char s[34];
    printf("Enter your name: ");
    scanf("%s",s); //scanf automatically adds the null character.
    //Note that "&" is not used here because s is an array which is already a address.
    printf("Your name is %s",s);
    return 0;
    
    

    return 0;
}
//1. scanf automatically adds the null character when the enter key is pressed.
//2.The string should be short enough to fit into the array.
//3.scanf cannot be used to input multi-word strings with spaces.
//4.gets() is a function which can be used to take multiword strings.
